Created At: 2026-05-25T22:52:14Z
Completed At: 2026-05-25T22:52:14Z
The following changes were made by the multi_replace_file_content tool to: c:\trabajos\test\test13\app\src\screens\main\DashboardScreen.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -10,9 +10,12 @@
 import { walletsApi } from '@/api/wallets.api';
 import { ticketsApi } from '@/api/tickets.api';
 import { processWalletsWithTickets } from '@/utils/walletCalculations';
-
-export const NewDashboardScreen: React.FC = () => {
-  const navigation = useNavigation();
+import { useAuthStore } from '@/store/auth.store';
+import { RecentMovementsPanel } from '@/components/dashboard/RecentMovementsPanel';
+
+export const DashboardScreen: React.FC = () => {
+  const navigation = useNavigation<any>();
+  const { user } = useAuthStore();
   const [wallets, setWallets] = useState<LocalWallet[]>([]);
   const [tickets, setTickets] = useState<LocalTicket[]>([]);
   const [refreshing, setRefreshing] = useState(false);
@@ -153,10 +153,9 @@
   return (
     <SafeAreaView style={styles.container}>
       <View style={styles.header}>
-        <TouchableOpacity onPress={() => navigation.goBack()} style={styles.backButton}>
-          <Ionicons name="chevron-back" size={24} color={Colors.textPrimary} />
-        </TouchableOpacity>
-        <Typography variant="headingH3" style={styles.title}>Resumen</Typography>
+        <View style={{ flex: 1 }}>
+          <Typography variant="headingH2">Hola, {user?.displayName || "Usuario"} 👋</Typography>
+        </View>
         <View style={{ width: 40 }} />
       </View>
       
@@ -303,6 +303,16 @@
             </Typography>
           </View>
         )}
+
+        {/* SECCION: ÚLTIMOS MOVIMIENTOS */}
+        <View style={{ marginTop: 24 }}>
+          <RecentMovementsPanel
+            movements={tickets.slice(0, 10)}
+            userPhoneNumber={user?.phoneNumber || undefined}
+            onPressItem={(id) => navigation.navigate("AddMovementModal", { ticketId: id })}
+            onPressViewAll={() => navigation.navigate("Historial")}
+          />
+        </View>
       </ScrollView>
     </SafeAreaView>
   );
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.